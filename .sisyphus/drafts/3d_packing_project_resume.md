# 孙晓萌 - 个人简历

## 联系方式
- 📧 Email: [你的邮箱]
- 📱 Phone: [你的电话]
- 💼 GitHub: [你的GitHub]
- 🌐 Portfolio: [你的网站]

---

## 技术技能

### 编程语言
- **后端**: Go (1.23+), Python (3.10+), TypeScript
- **前端**: React (18+), TypeScript, JavaScript (ES6+)
- **查询语言**: SQL, GraphQL

### 框架与库
- **后端框架**: go-zero, CloudWeGO Eino, FastAPI, Celery
- **前端框架**: React, Vite, Zustand, Tailwind CSS
- **3D渲染**: Three.js, @google/model-viewer
- **AI/ML**: Hunyuan3D, FLUX.2, SDXL Turbo, Ollama, Milvus

### 数据库与存储
- **关系型**: MySQL 8.0+, PostgreSQL
- **NoSQL**: Redis, Milvus (向量数据库)
- **对象存储**: MinIO

### 基础设施
- **容器化**: Docker, Docker Compose
- **编排工具**: Kubernetes (基础)
- **消息队列**: Redis + Celery
- **CI/CD**: GitHub Actions (基础)

### 其他技能
- **系统设计**: 微服务架构、分布式系统、高可用设计
- **API设计**: RESTful API、GraphQL、WebSocket、SSE
- **开发工具**: Git, Linux, Vim/VSCode
- **项目管理**: 需求分析、技术选型、团队协作

---

## 项目经历

### 🚀 2026年01月 - 3D包装设计AI生成平台

**项目周期**: 2026年01月14日 - 2026年01月30日 (16天)

**项目角色**: 全栈架构师 & 主开发工程师

**项目概述**:

开发了一套面向包装设计领域的端到端AI可视化平台。用户可通过自然语言描述需求（如"设计一个红色能量饮料瓶包装"），平台自动完成关键词提取、参考图检索、纹理生成、3D模型创建及纹理应用，最终输出带纹理的3D模型和渲染图。该平台整合了腾讯混元3D、FLUX.2、SDXL Turbo等多个业界领先的AI生成模型，通过微服务架构实现高并发、高可用的AI推理服务。

**技术架构**:

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户访问层 (Web浏览器)                          │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     前端层 (React + TypeScript)                       │
│  • React 18.3.1 + TypeScript 5.6                                     │
│  • Vite 5.4.10 构建工具                                              │
│  • Tailwind CSS 3.4.14 样式框架                                      │
│  • Zustand 5.0.10 状态管理                                           │
│  • Axios 1.7.7 HTTP客户端                                            │
│  • SSE实时通信实现进度推送                                            │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                    HTTP请求 / SSE事件流
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   Agent智能编排层 (Go + go-zero)                      │
│  • Go 1.23.5 微服务框架                                              │
│  • go-zero v1.9.4 RESTful API框架                                    │
│  • CloudWeGO Eino v0.7.18 AI工作流编排                               │
│  • go-frame v2.6.4 ORM框架                                           │
│  • Ollama (bge-m3嵌入 + qwen2.5:8b对话模型)                          │
│  • Milvus 2.4+ 向量数据库                                             │
│  • MySQL 8.0 任务与资产数据存储                                       │
│  • Redis 5.0 会话状态与缓存                                          │
│  • Docker Compose 服务编排                                           │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   API网关层 (FastAPI + Python)                        │
│  • FastAPI 0.109.0 Web框架                                           │
│  • 统一入口转发到各子服务                                             │
│  • 静态文件代理服务                                                   │
│  • 文件上传处理 (图片/3D模型)                                         │
│  • CORS跨域支持                                                      │
└─────────────────────────────────────────────────────────────────────┘
                                 │
              ┌──────────────────┼──────────────────┐
              ▼                  ▼                  ▼
    ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
    │ GPU 0 (Hunyuan) │  │ GPU 1 (FLUX) │   │ GPU 2 (SDXL) │
    │ • 3D模型生成    │  │ • 纹理生成    │   │ • 纹理生成    │
    │ • 纹理应用      │  │ • 多图融合    │   │ • 变体生成    │
    └──────────────┘   └──────────────┘   └──────────────┘
```

**核心功能模块**:

#### 1. 智能提示词解析系统
- **功能描述**: 用户输入自然语言描述后，系统自动调用LLM进行语义分析，提取结构化关键词
- **技术实现**:
  - 使用Ollama qwen2.5:8b进行提示词理解和关键词提取
  - 提取维度包括：颜色(Color)、风格(Style)、元素(Elements)、材质(Texture)、情绪(Mood)、模型类型(ModelType)
  - 模型类型自动识别（瓶子、盒子、袋子、罐子等）
  - 提示词完整度评分与优化建议
- **关键代码**: `NodeKeywordExtractor` (Eino Graph节点)

#### 2. 参考图检索系统
- **功能描述**: 基于用户确认的关键词，从向量数据库中检索相似参考图
- **技术实现**:
  - 使用Ollama bge-m3生成文本嵌入向量
  - Milvus 2.4+向量相似性检索
  - 多维度评分（相似度、描述匹配度）
  - 支持用户多选参考图用于后续纹理生成
- **数据流**: 关键词 → 嵌入向量 → Milvus检索 → 参考图推荐

#### 3. AI纹理生成引擎
- **功能描述**: 根据用户提示词和参考图生成无缝包装纹理
- **技术实现**:
  - **主模型**: SDXL Turbo + FLUX.2 [klein] 9B
  - **控制网络**: ControlNet Tile模式确保无缝平铺
  - **多图融合**: 支持多参考图条件生成
  - **分辨率**: 支持4K+高分辨率输出
  - **生成策略**: 
    - 单图模式：根据文本生成
    - 融合模式：多参考图加权融合
    - 变体模式：基于已有纹理生成变体
- **API接口**: `POST /api/v1/texture/generate`
- **服务端口**: 8801 (SDXL), 8805 (FLUX.2)

#### 4. 3D模型生成与处理系统
- **功能描述**: 根据用户需求生成或处理3D包装模型
- **技术实现**:
  - **几何生成**: Hunyuan3D-DiT (腾讯混元3D 2.1)
    - 支持文本→3D模型 (txt2mesh)
    - 支持图像→3D模型 (img2mesh)
    - 超高分辨率几何 (1536³)
  - **纹理应用**: Hunyuan3D-Paint
    - PBR材质生成 (Albedo, Roughness, Metallic, Normal)
    - 自定义纹理+提示词应用
  - **用户上传**: 支持GLB/OBJ/FBX/PLY/STL格式
  - **UV处理**: xatlas UV展开
- **任务队列**: Redis + Celery (4 Workers: hunyuan, uv, flux2, easitex)
- **GPU分配**: Hunyuan3D专用GPU 0

#### 5. 3D交互预览系统
- **功能描述**: 在Web端实时预览生成的3D模型
- **技术实现**:
  - **GLB/GLTF格式**: @google/model-viewer组件
    - 自动旋转、缩放、拖拽控制
    - 多种视角预设
    - 加载动画与进度指示
  - **OBJ格式**: Three.js自定义渲染
    - MTL材质文件解析与应用
    - 光照系统 (环境光+方向光)
    - 相机轨道控制
  - **多格式支持**: .glb, .gltf, .obj, .mtl, .png
- **交互功能**:
  - 自动旋转展示
  - 缩放滑块控制
  - 视角重置按钮
  - 截图保存功能

#### 6. 任务管理与实时通信
- **功能描述**: 实时推送任务进度、排队位置、预计等待时间
- **技术实现**:
  - **SSE (Server-Sent Events)**: 
    - 纹理生成进度: `/api/v1/texture/events/{sessionId}`
    - 模型生成进度: `/api/v1/model/events/{sessionId}`
  - **事件类型**: connected, progress, result, completed, error
  - **排队管理**:
    - Redis存储队列位置
    - 预计等待时间计算
    - 任务取消与重试
  - **状态恢复**: 断线重连后自动恢复状态

#### 7. 结果导出系统
- **功能描述**: 一键打包下载生成的所有资源
- **技术实现**:
  - **ZIP打包**: JSZip 3.10.1
  - **包含内容**:
    - 3D模型文件 (.glb)
    - 纹理图片 (.png)
    - 材质定义 (.mtl)
    - 元数据文件 (info.json)
  - **文件大小**: 支持最大500MB
  - **下载方式**: FileSaver.js触发浏览器下载

**工作流程设计**:

```
┌──────────────────────────────────────────────────────────────────────────┐
│                          用户交互工作流                                    │
└──────────────────────────────────────────────────────────────────────────┘

步骤 1: 用户输入
  ┌─────────────────────────────────────────────────────────────────────┐
  │  用户在PromptInput组件中输入自然语言描述                             │
  │  示例: "设计一个红色能量饮料瓶包装，银色logo，碳纤维背景"             │
  │  功能: 实时完整度评分、优化建议、示例模板                             │
  └─────────────────────────────────────────────────────────────────────┘
         │
         ▼
步骤 2: 关键词提取 (LLM调用)
  ┌─────────────────────────────────────────────────────────────────────┐
  │  POST /api/v1/texture/generate (step: "extract")                    │
  │  响应: {                                                            │
  │    "keywords": {                                                    │
  │      "Colors": ["红色", "银色"],                                     │
  │      "Style": ["现代", "科技感"],                                    │
  │      "Elements": ["logo", "碳纤维纹理"],                             │
  │      "Texture": ["光滑", "金属质感"],                                │
  │      "Mood": ["能量", "活力"],                                       │
  │      "ModelType": "瓶子"                                             │
  │    },                                                               │
  │    "texture_query": "red energy drink bottle packaging...",         │
  │    "model_type": "bottle"                                            │
  │  }                                                                  │
  └─────────────────────────────────────────────────────────────────────┘
         │
         ▼
步骤 3: 用户确认关键词
  ┌─────────────────────────────────────────────────────────────────────┐
  │  KeywordsReview组件展示提取的关键词                                  │
  │  用户可: 确认、修改、删除关键词                                       │
  │  确认后进入下一步                                                    │
  └─────────────────────────────────────────────────────────────────────┘
         │
         ▼
步骤 4: 参考图检索 (Milvus向量检索)
  ┌─────────────────────────────────────────────────────────────────────┐
  │  POST /api/v1/texture/generate (step: "retrieve")                   │
  │  基于confirmed_keywords检索参考图                                    │
  │  响应: { "reference_images": [...], "estimated_time": 5 }           │
  └─────────────────────────────────────────────────────────────────────┘
         │
         ▼
步骤 5: 用户选择参考图
  ┌─────────────────────────────────────────────────────────────────────┐
  │  ReferenceSelector组件展示推荐参考图                                 │
  │  用户可多选参考图用于纹理生成                                        │
  │  支持相似度评分和描述查看                                            │
  └─────────────────────────────────────────────────────────────────────┘
         │
         ▼
步骤 6: 纹理生成 (SDXL/FLUX)
  ┌─────────────────────────────────────────────────────────────────────┐
  │  POST /api/v1/texture/generate (step: "generate")                   │
  │  SSE监听: /api/v1/texture/events/{sessionId}                        │
  │  事件流: progress → result → completed                              │
  │  输出: texture_url (PNG图片URL)                                      │
  └─────────────────────────────────────────────────────────────────────┘
         │
         ▼
步骤 7: 纹理预览与确认
  ┌─────────────────────────────────────────────────────────────────────┐
  │  TexturePreview组件展示生成的纹理                                    │
  │  用户可: 确认、取消、重试                                            │
  │  确认后进入模型选择步骤                                              │
  └─────────────────────────────────────────────────────────────────────┘
         │
         ▼
步骤 8: 模型选择 (生成 或 上传)
  ┌─────────────────────────────────────────────────────────────────────┐
  │  ModelOptionSelector组件                                            │
  │  选项A: 生成模型 (调用Hunyuan3D)                                     │
  │  选项B: 上传模型 (支持GLB/OBJ等格式)                                 │
  └─────────────────────────────────────────────────────────────────────┘
         │
         ├── 选项A: 模型生成 ──────────────────────────────────────────┐
         │  ┌─────────────────────────────────────────────────────────┐│
         │  │  POST /api/v1/model/process (source_type: "generate")  ││
         │  │  SSE监听: /api/v1/model/events/{sessionId}             ││
         │  │  输出: model_url (GLB文件URL)                          ││
         │  └─────────────────────────────────────────────────────────┘│
         │         │                                                      │
         ▼         ▼                                                      ▼
  ┌─────────────────────────────────────────────────────────────────────┐
  │  ModelPreview组件: 3D模型交互预览                                    │
  │  - @google/model-viewer渲染GLB                                      │
  │  - Three.js渲染OBJ+MTL                                              │
  │  - 自动旋转、缩放、拖拽                                              │
  └─────────────────────────────────────────────────────────────────────┘
         │
         ▼
步骤 9: 完成与导出
  ┌─────────────────────────────────────────────────────────────────────┐
  │  Completion组件展示最终结果                                          │
  │  - 3D模型展示 (旋转动画)                                             │
  │  - 纹理图展示                                                        │
  │  - 设计理念文案 (LLM生成)                                            │
  │  - 技术指标 (格式、大小、分辨率)                                     │
  │  - ZIP打包下载 (JSZip)                                               │
  └─────────────────────────────────────────────────────────────────────┘
```

**API接口设计**:

#### Agent层 API (端口 8888)

```go
// 纹理生成工作流
POST /api/v1/texture/generate
// 请求体:
{
  "session_id": "string",
  "step": "extract" | "retrieve" | "generate",
  "prompt": "string",
  "texture_query": "string",
  "confirmed_keywords": {
    "Colors": ["string"],
    "Style": ["string"],
    "Elements": ["string"]
  },
  "selected_images": [{"texture_id": 1, "selected": true}]
}

// 响应:
{
  "task_id": "TEX-xxx",
  "session_id": "string",
  "current_step": "string",
  "next_step": "string",
  "keywords": {...},
  "reference_images": [...],
  "texture_url": "string",
  "estimated_time": 30
}

// SSE事件流
GET /api/v1/texture/events/{sessionId}
// 事件格式:
{
  "type": "connected" | "progress" | "result" | "completed" | "error",
  "progress": 50,
  "status": "生成中",
  "texture_url": "string",
  "queue_position": 2,
  "estimated_time": 120
}
```

```go
// 模型处理工作流
POST /api/v1/model/process
// 请求体:
{
  "session_id": "string",
  "step": "generate_3d",
  "source_type": "generate" | "upload",
  "texture_prompt": "string",
  "prompt": "string",
  "file_id": "string",
  "user_upload_model_id": "string"
}
```

```go
// 任务管理
GET /api/v1/users/{userId}/tasks
// 响应:
{
  "tasks": [
    {
      "task_id": "string",
      "task_status": "1" | "5" | "6" | "7" | "8",  // 状态码
      "prompt": "string",
      "texture_generate_result": "string",
      "model_generate_result": "string",
      "created_at": "string"
    }
  ]
}
```

#### API Gateway API (端口 8808)

```python
# 子服务转发
POST /api/v1/sdxl/txt2img          # SDXL纹理生成
POST /api/v1/flux2/txt2img         # FLUX纹理生成
POST /api/v1/hunyuan/generate      # Hunyuan3D生成
POST /api/v1/hunyuan/img2textured  # Hunyuan纹理应用
POST /api/v1/uv/unwrap             # UV展开
POST /api/v1/paint3d/generate      # Paint3D纹理应用
POST /api/v1/paint3d/pbr           # PBR贴图生成
POST /api/v1/easitex/texture       # EASI-TEX纹理

# 文件上传
POST /api/v1/upload/image          # 上传图片
POST /api/v1/upload/mesh           # 上传3D模型

# 健康检查
GET /health                        # 网关健康检查
```

**前端技术实现**:

```typescript
// 技术栈详情
{
  "frontend": {
    "framework": "React 18.3.1",
    "language": "TypeScript 5.6.2",
    "buildTool": "Vite 5.4.10",
    "styling": "Tailwind CSS 3.4.14",
    "3DRendering": ["Three.js 0.182.0", "@google/model-viewer 4.1.0"],
    "stateManagement": "Zustand 5.0.10",
    "httpClient": "Axios 1.7.7",
    "fileHandling": ["JSZip 3.10.1", "file-saver 2.0.5"],
    "realtime": "Server-Sent Events (SSE)"
  }
}

// 状态管理 (Zustand)
interface WorkflowState {
  // 会话信息
  sessionId: string | null;
  
  // 工作流步骤
  currentStep: WorkflowStep;  // idle → extract → retrieve → generate → model_selection → model_generate → completed
  
  // 纹理生成状态
  textureUrl: string | null;
  textureProgress: number;
  textureStatus: string;  // pending → processing → completed
  textureQueuePosition: number | null;
  
  // 模型生成状态
  modelUrl: string | null;
  mtlUrl: string | null;
  pngUrl: string | null;
  modelProgress: number;
  modelStatus: string;
  modelQueuePosition: number | null;
  
  // 数据
  keywords: Keywords | null;
  confirmedKeywords: Partial<Keywords>;
  referenceImages: ReferenceImage[];
  selectedImages: ReferenceImage[];
}

// SSE事件处理
const handleSSEEvent = (event: SSEEvent) => {
  switch (event.type) {
    case 'connected':
      console.log('SSE连接建立');
      break;
    case 'progress':
      updateProgress(event.progress, event.status);
      updateQueueInfo(event.queue_position, event.estimated_time);
      break;
    case 'result':
      setTextureUrl(event.texture_url);
      break;
    case 'completed':
      setCurrentStep(event.next_step);
      break;
    case 'error':
      showError(event.error);
      break;
  }
};
```

**后端技术实现**:

```go
// Agent层技术栈
{
  "backend": {
    "language": "Go 1.23.5",
    "webFramework": "go-zero v1.9.4",
    "aiOrchestration": "CloudWeGO Eino v0.7.18",
    "orm": "go-frame v2.6.4",
    "vectorDB": "Milvus 2.4+",
    "llm": {
      "embedding": "Ollama bge-m3",
      "chat": "Ollama qwen2.5:8b"
    },
    "database": ["MySQL 8.0+", "Redis 5.0+"],
    "container": "Docker Compose"
  }
}

// Eino Graph编排示例
type DesignConceptGraph struct {
  graph *compose.Graph[string, *WorkflowState]
}

func NewDesignConceptGraph(...) *DesignConceptGraph {
  g := compose.NewGraph[string, *WorkflowState]()
  
  // 节点定义
  extractor := nodes.NewKeywordExtractor(chatModel)
  embedder := nodes.NewEmbedding(embedding)
  retriever := nodes.NewMilvusRetriever(milvusClient)
  
  // 边连接
  g.AppendNodes(extractor, embedder, retriever)
  
  return &DesignConceptGraph{graph: g}
}
```

```python
# AI服务层技术栈
{
  "ai_services": {
    "language": "Python 3.10+",
    "webFramework": "FastAPI 0.109.0",
    "taskQueue": "Celery 5.3.6 + Redis 5.0.1",
    "3dModels": {
      "hunyuan3d": "Hunyuan3D 2.1 (腾讯混元3D)",
      "flux2": "FLUX.2 [klein] 9B",
      "sdxl": "SDXL Turbo",
      "paint3d": "Paint3D",
      "easitex": "EASI-TEX"
    },
    "uvProcessing": "xatlas",
    "gpuScheduling": "多GPU动态分配"
  }
}

// Celery任务队列配置
from celery import Celery

app = Celery('3d_packing', broker='redis://localhost:6379/0')

@app.task(bind=True, max_retries=3)
def hunyuan_generate_task(self, prompt: str, texture_url: str):
    try:
        # 调用Hunyuan3D推理服务
        result = hunyuan_client.generate(
            prompt=prompt,
            texture=texture_url,
            gpu_id=0  # GPU 0专用
        )
        return result
    except Exception as e:
        self.retry(exc=e, countdown=60)
```

**项目亮点**:

1. **端到端AI自动化**: 从自然语言输入到3D模型输出的完整闭环，用户无需任何专业技能即可生成专业级包装设计方案。

2. **多模型协同工作**: 整合业界领先的多个生成模型（腾讯混元3D、FLUX.2、SDXL Turbo），每个模块使用最适合的模型。

3. **实时交互体验**: 采用SSE技术实现秒级进度推送，用户可实时查看生成进度、排队位置、预计等待时间。

4. **生产级架构**: 三层微服务分离设计，支持独立扩缩容；Docker Compose一键部署；多GPU负载均衡。

5. **用户友好的工作流**: 清晰的步骤引导，从提示词输入到最终导出，每一步都有明确的反馈和操作指引。

6. **3D预览体验**: 集成Two.js和@google/model-viewer，支持多种3D格式，提供流畅的交互预览体验。

**项目成果**:

- ✅ 完成16天完整开发周期，按时交付
- ✅ 实现从0到1的端到端AI生成系统
- ✅ 支持多用户并发访问
- ✅ 生成质量达到工业级标准（PBR材质）
- ✅ 用户可通过直观界面完成复杂3D设计任务

**项目挑战与解决方案**:

1. **挑战**: 多模型集成复杂，API接口不统一
   - **解决**: 设计API Gateway统一入口，转发请求到各子服务

2. **挑战**: AI生成耗时较长（30s-5min）
   - **解决**: 实现SSE实时推送进度和排队信息，提升用户体验

3. **挑战**: 3D模型格式多样，渲染兼容性问题
   - **解决**: 同时支持@google/model-viewer（GLB）和Three.js（OBJ+MTL）两种渲染方案

4. **挑战**: GPU资源有限，多用户竞争
   - **解决**: 实现Redis队列管理，支持任务排队和取消

5. **挑战**: 前后端状态同步
   - **解决**: 使用Zustand集中管理状态，SSE事件驱动更新

**项目关系图**:

```
3D包装设计AI生成平台 (完整项目)
│
├── /home/sunxiaomeng/3d_packing/ (AI服务层)
│   ├── Python + FastAPI + Celery
│   ├── Hunyuan3D 2.1 (3D模型生成)
│   ├── FLUX.2 [klein] 9B (纹理生成)
│   ├── SDXL Turbo (纹理生成)
│   ├── Paint3D (纹理应用)
│   ├── EASI-TEX (纹理生成)
│   ├── UV展开服务
│   └── API Gateway (统一入口)
│
├── /home/sunxiaomeng/3d_packing_agent/ (Agent智能编排层)
│   ├── Go 1.23 + go-zero
│   ├── CloudWeGO Eino (AI编排)
│   ├── Ollama (LLM集成)
│   ├── Milvus (向量检索)
│   ├── MySQL + Redis
│   └── Docker Compose (服务编排)
│
└── /home/sunxiaomeng/3d_packing_frontend/ (前端展示层)
    ├── React 18 + TypeScript
    ├── Vite + Tailwind CSS
    ├── Three.js + @google/model-viewer
    ├── Zustand (状态管理)
    └── Axios + SSE (网络通信)
```

**代码位置**:

- 前端代码: `/home/sunxiaomeng/3d_packing_frontend/`
- Agent代码: `/home/sunxiaomeng/3d_packing_agent/go_code/`
- AI服务代码: `/home/sunxiaomeng/3d_packing/`
- API网关: `/home/sunxiaomeng/3d_packing/interfaces/api_server.py`

---

## 教育背景

**学历**: [你的学历]
**学校**: [你的学校]
**专业**: [你的专业]
**毕业时间**: [毕业时间]

---

## 荣誉与奖项

- [荣誉奖项1]
- [荣誉奖项2]

---

## 自我评价

我是一名全栈开发工程师，专注于AI应用开发和分布式系统架构。在3D包装设计AI生成平台项目中，我从零开始设计并实现了完整的三层微服务架构，整合了多个业界领先的AI生成模型，实现了从自然语言到3D模型的端到端自动化生成。

我的技术特点：
- **系统架构设计能力**: 能够设计高可用、可扩展的微服务架构
- **全栈开发能力**: 精通Go、Python、TypeScript等多种语言
- **AI集成能力**: 熟悉主流生成AI模型的集成和应用
- **问题解决能力**: 善于将复杂问题拆解并逐一解决
- **学习能力**: 快速学习和应用新技术

---

*最后更新: 2026年02月*
